export interface IData {
  email: string;
  name: string;
  credits: number;
}

export interface ICardInfo {
  image: string;
  title: string;
}

export interface IExpart {
  avatar: string;
  name: string;
}
